# Core dependencies
streamlit==1.32.0
pandas==2.2.1
numpy==1.26.4
Pillow==10.2.0
pytesseract==0.3.10
python-dotenv==1.0.1

# UI components
streamlit-option-menu==0.3.12
streamlit-card==1.0.0
streamlit-extras==0.3.6
streamlit-lottie==0.0.5
streamlit-toggle==0.1.1
streamlit-authenticator==0.3.1

# Translation and audio
gTTS==2.5.1
SpeechRecognition==3.10.1
pyaudio>=0.2.13  # Required for voice input
playsound>=1.3.0
langdetect
# Utility packages
requests==2.31.0
keyboard==0.13.5
pyperclip==1.8.2
plotly==5.19.0

# API integration
python-json-logger>=2.0.7

# System dependencies (commented - need to be installed separately on the system)
# For Ubuntu/Debian:
# sudo apt-get install portaudio19-dev python3-pyaudio
# sudo apt-get install tesseract-ocr

# For Windows:
# Ensure Tesseract-OCR is installed and in PATH
# Install PyAudio wheel if pip install fails

[server]
enableXsrfProtection = false
enableCORS = false
